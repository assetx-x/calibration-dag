slack {
    token = test
    token = ${?SLACK_API_TOKEN}
    app_name = "DCM Systems Health Check"
    colors = {
        default = "grey"
        good = "good"
        warning = "warning"
        error = "danger"
    }
}
